<div class="search">
  <%= render :partial => 'search' %>
</div>


<table class="table table-striped table-hover">
    <tr>
        <th></th>
        <th>Nome</th>
        <th>CPF</th>
        <th>Número USP</th>
        <th>Tipo</th>
        <th>E-mail</th>
        <th></th>
        <th></th>
    </tr>
    <% if (current_user.kind == "admin") || (current_user == @user) %>
    <% @users.each do |user| %>
        <tr>
            <td>
               <%= user.id %>
            </td>
            <td>
                <%= user.name %>
            </td>
            <td>
               <%= user.cpf %>
            </td>
            <td>
               <%= user.nusp %>
            </td>
            <td>
                <%= user.kind %>
            </td>
            <td>
                <%= user.email%>
            </td>
            <td>
                <%= link_to "Visualizar", user_path(:id => user.id)  %>
            </td>
            <td>
                <%= link_to "Editar Dados", edit_user_path(:id => user.id)  %>
           </td>
        </tr>
    <% end -%>
    <% else -%>
        <tr>
            <td>
            </td>
            <td>
                <%= current_user.try(:name) %>
            </td>
            <td>
                <%= current_user.try(:cpf) %>
            </td>
            <td>
                <%= current_user.try(:nusp) %>
            </td>
            <td>
                <%= current_user.try(:kind) %>
            </td>
            <td>
                <%= current_user.try(:email) %>
            </td>
            <td>
                    <%= link_to "Visualizar", user_path(:id => current_user.try(:id))  %>
            </td>
            <td>
              <%= link_to "Editar Dados", edit_user_path(:id => current_user.try(:id))  %>
           </td>
        </tr>
        <% end -%>
</table>