<table class="table table-striped table-hover">
    <tr>
        <th>Nome</th>
        <th>Número de Vistorias</th>
        <th></th>
    </tr>
    <% @blds.each do |bld| %>
        <tr>
            <td>
               <%= label "", bld.name, "" %>
            </td>
            <td>
	      	<% count = Group.where(building_id: bld.id).count %>
	      	<% if count > 0 %>
               		<%= link_to count , buildings_groups_detail_path(id: bld.id) %>
		<% else %>
			<%= label "", 0 , "" %>
		<% end %>
            </td>
        </tr>
    <% end -%>
</table>
