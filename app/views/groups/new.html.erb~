<%= nested_form_for @group do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :building_id, "Prédio"%>
  <%= f.select(:building_id, options_for_select(@predios, @predio)) %>

  <%= f.label :vist_date, "Data"%>
  <%= f.date_field :vist_date %>

  <br/>
  <%= f.submit "Criar" %>
  
<%end%>
